# Production Environment Configuration
# ⚠️ IMPORTANT: Generate new secure values for production!

# OpenAI API Key (keep your existing key)
OPENAI_API_KEY=sk-proj-YOUR_PRODUCTION_KEY_HERE

# Database Configuration
# For production, use PostgreSQL instead of SQLite
DATABASE_URL=postgresql://username:password@your-db-host:5432/appsec_prod
# Alternative: Keep SQLite but use absolute path
# DATABASE_URL=sqlite:////var/app/data/appsec.db

# Security Settings - MUST CHANGE THESE!
SECRET_KEY=GENERATE_A_STRONG_RANDOM_SECRET_KEY_HERE_AT_LEAST_32_CHARS
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Settings - Replace with your production domain
CORS_ORIGINS=https://your-domain.com,https://api.your-domain.com

# Production Settings
ENVIRONMENT=production
DEBUG=false

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Optional: Rate Limiting
RATE_LIMIT_PER_MINUTE=100

# Optional: Logging
LOG_LEVEL=INFO
